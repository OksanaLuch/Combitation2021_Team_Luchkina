#include <string>
#include <iostream>
#include <functional>
#include <vector>
#include <chrono>

using namespace std;

vector<vector<int>> combo(int k, vector<int> test);
vector<vector<int>> GrayCode(int k, vector<int> test);
vector<vector<int>> LexicographicOrder(int k, vector<int> test);
int C(int k, int n);
